{
  "name": "subframe-bundle-arena",
  "type": "module",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun --filter './apps/*' dev",
    "build": "bun --filter './apps/*' build",
    "preview": "bun --filter './apps/*' preview",
    "analyze": "bun --filter './apps/*' analyze"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
